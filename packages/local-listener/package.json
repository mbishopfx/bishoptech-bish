{
  "name": "@bish/local-listener",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "tsup src/index.ts --format esm --dts",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@bish/automation": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.9.3"
  }
}
